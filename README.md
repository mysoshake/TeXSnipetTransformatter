# TeX Snipet Transformatter
TeXのソースから、スニペット用の各行を生み出すもの

## 手順
1. ソースをコンパイル(classがあるので不要)
2. テキストファイルを指定して実行
3. それらしいファイルが生成される

## 手順(詳しく)
1. TJ.classを好きな場所に配置
2. TJ.classがあるディレクトリでコマンドプロンプトなどを起動
3. `java TJ (ファイル名).tex`などのように，ファイル名を指定して実行する
4. うまくいけば`output_(ファイル名).tex`のようなファイルが生成される
